version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - echo Building SpotifAI StreamSmart
        - node build-env.js
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
    exclude:
      - node_modules/**/*
